<Window x:Class="VoicemeeterDelay.RouteEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Routes"
        Width="540"
        Height="430"
        MinWidth="500"
        MinHeight="320"
        Background="#111417"
        FontFamily="Segoe UI"
        FontSize="13"
        Icon="Assets/VoicemeeterDelay.ico"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock x:Name="TitleTextBlock"
               Grid.Row="0"
               Text="Routes"
               FontSize="18"
               FontWeight="SemiBold"
               Foreground="#55C27A"
               Margin="0,0,0,12" />

    <Border Grid.Row="1"
            Background="#11171B"
            BorderBrush="#33414A"
            BorderThickness="1"
            CornerRadius="6"
            Padding="10">
      <ScrollViewer VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled">
        <StackPanel x:Name="RoutesPanel" />
      </ScrollViewer>
    </Border>

    <Grid Grid.Row="2"
          Margin="0,12,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <Button x:Name="AddButton"
              Grid.Column="0"
              Content="Add"
              Width="90"
              MinHeight="30"
              HorizontalAlignment="Left"
              Background="#14392F"
              Foreground="#EDF3F6"
              BorderBrush="#55C27A"
              ToolTip="Add another output destination for this source channel."
              Click="AddButton_Click" />
      <Button Grid.Column="1"
              Content="Close"
              Width="90"
              MinHeight="30"
              Background="#27313A"
              Foreground="#EDF3F6"
              BorderBrush="#33414A"
              ToolTip="Close this route editor. Route changes are already saved."
              Click="CloseButton_Click" />
    </Grid>
  </Grid>
</Window>
